<div id="head"> <a href="/"><h1>SAWIT Educate To Empower</h1></a> </div>
<div id="adminpan">
    {{#if administrator}}
    <div class="container">

        <table class="table table-bordered">
            <thead>
                <tr>
                    <th class="col-lg-4">Username</th><th class="col-lg-4">Admin</th><th class="col-lg-4">Locked</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    {{#users}}
                    <form action="/user/admin_panel/{{username}}" method="POST">
                <tr>
                    <td>
                        <input type="text" name="username" class="col-lg-12" value="{{username}}" readOnly>
                    </td>
                    <td>
                    <select name="admin">
                        {{#if admin}}
                        <option value="{{admin}}">admin</option>
                        <option value="0">Not Admin</option>
                        {{else}}
                        <option value="{{admin}}">Not Admin</option>
                        <option value="1">Admin</option>
                        {{/if}}
                    </select>
                    </td>
                    <td>
                    <select name="locked">
                        {{#if locked}}
                        <option value="{{locked}}">Locked</option>
                        <option value="0">UnLocked</option>
                        {{else}}
                        <option value="{{locked}}">Not Locked</option>
                        <option value="1">Locked</option>
                        {{/if}}
                    </select>
                    </td>
                    <td>
                        <input id="adm" type="submit" value="Submit">
                    </td>
                </tr>
                    </form>
                    {{/users}}
            </tbody>
    </table>
</div>
{{else}}
      <h1>YOU HAVE TO BE ADMININISTRATOR!</h1>
{{/if}}
</div>
